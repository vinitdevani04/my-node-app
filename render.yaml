services:
  - type: web
    name: my-node-app
    env: node
    plan: free
    buildCommand: 'npm install'
    startCommand: 'npm start'
    envVars:
      - key: MONGODB_URI
        value: 'mongodb://localhost:27017/'