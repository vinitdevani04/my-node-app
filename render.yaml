services:
  - type: web
    name: my-node-app
    env: node
    plan: free
    buildCommand: 'npm install'
    startCommand: 'node index.js'
    envVars:
      - key: MONGODB_URI
        value: "mongodb://localhost:27017/AppleDB"